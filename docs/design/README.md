# UIデザイン（Pencil）

本ディレクトリは、メモアプリのUIデザイン成果物を管理する。
デザインは **Pencil** を使用して作成し、ドキュメント駆動開発の一環として `docs/` 配下に配置する。

- **デザイン方針・トークン・画面別指針**: [`design.md`](./design.md)
- **モックアップファイル**: [`design.pen`](./design.pen)
- **本ドキュメント**: ファイル配置・編集ワークフロー等の運用ルール

---

## 1. Pencil とは

[Pencil Project](https://pencil.evolus.vn/) は、オープンソースのGUIプロトタイピング・ワイヤーフレームツール。

- ワイヤーフレーム / モックアップを軽量に作成できる
- 成果物はファイルとして保存され、Gitで差分管理可能（ただしバイナリ寄りなのでマージは困難）
- 画像（PNG / SVG）へのエクスポートに対応

---

## 2. ファイル構成

```
docs/design/
├── README.md          # 本ファイル（運用ルール）
├── design.md          # デザイン方針・トークン・画面別指針
├── design.pen         # メインのモックアップ（全画面集約）
└── assets/            # エクスポートしたPNG等（必要になったら作成）
```

### 2.1 ファイル分割方針

- **初期は `design.pen` 1ファイルに全画面を集約する**
- 画面数が増えて1ファイルが扱いにくくなった場合は、画面単位で分割する
  - 例: `memo-list.pen` / `memo-edit.pen` / `tag-manage-modal.pen` / `profile.pen`
- 分割した場合は本READMEのファイル構成セクションを更新する

---

## 3. デザイン対象画面（フェーズ1）

`design.pen` のトップレベルフレームを以下 4 区分で整理する（実装着手時はここを起点に該当フレームへ辿ること）。

### 3.1 リファレンス／カタログ

| フレーム名 | 役割 |
|---|---|
| Design System | デザイントークン（変数）と再利用コンポーネント（24 種）を一覧で確認するカタログ |
| 状態バリエーション集 (PC 1280) | Buttons / Inputs / Tag Chips / Sidebar Nav / Icon Buttons の各状態（Normal/Hover/Focus/Active/Disabled/Loading/Error）を一覧化したリファレンス |

### 3.2 実装対象の主画面

`requirements.md` §5 に対応する画面・モーダル群。

| フレーム名 | パス / トリガー |
|---|---|
| メモ一覧 (PC 1280) | `/`（PC 1280px） |
| メモ一覧 (Tablet 768) | `/`（タブレット 768px） |
| メモ一覧 (Mobile 375) | `/`（モバイル 375px） |
| メモ新規作成 (PC 1280) | `/memos/new` |
| メモ詳細 (PC 1280) | `/memos/[id]`（閲覧モード） |
| プロフィール編集 (PC 1280) | `/profile` |
| タグ管理モーダル (PC 1280) | メモ一覧の「タグ管理」から起動するモーダル |
| 削除確認ダイアログ (PC 1280) | メモ詳細・編集の削除メニューから起動 |
| タグ選択コンボボックス展開 (PC 1280) | メモ作成・編集時のタグ選択ポップオーバー展開状態 |

### 3.3 状態バリエーション

実装時のエッジケース確認用。`design.md` の対応セクションを併せて参照。

| フレーム名 | 状態 |
|---|---|
| メモ一覧 (空状態 PC 1280) | メモ 0 件（初回利用 / 全削除後） |
| メモ一覧 (検索結果0件 PC 1280) | 検索 or フィルタ適用後の 0 件 |
| メモ一覧 (ローディング PC 1280) | データ取得中（SkeletonCard / Spinner 使用） |
| メモ新規作成 (エラー状態 PC 1280) | バリデーションエラー集約表示 |

### 3.4 補助フレーム

| フレーム名 | 用途 |
|---|---|
| Toast 表示位置 (PC 1280) | 保存成功などのトースト表示位置（PC: 右下マージン 24px） |
| Toast 表示位置 (Mobile 375) | トースト表示位置（モバイル: 上部マージン 16px） |

> 上記以外に、ピン留めトグル / ピン状態バリアントなどは「Design System」内の再利用コンポーネントとして整備済み（`PinToggle/Off` / `PinToggle/On`）。

---

## 4. 運用ルール

### 4.1 ドキュメント駆動の位置づけ

- **デザイン変更 → 実装** の順で進める（コード先行を避ける）
- 仕様（`requirements.md`）とデザイン（`design.pen`）が食い違う場合は、原則どちらかを正として揃える（先に決めたほうを正とし、もう一方を更新）

### 4.2 編集ワークフロー

1. Pencil で `docs/design/design.pen` を開く
2. 変更を加えて保存
3. 必要に応じて主要画面を PNG にエクスポートし `docs/design/assets/` に配置
4. Git にコミット
   - コミットメッセージ例: `docs: メモ一覧画面のレイアウトを更新`
   - 規約: `docs:` プレフィックス（CLAUDE.md §7.2 参照）

### 4.3 レビューのしやすさ

- `.pen` ファイルはバイナリ寄りで、PRレビュー時に差分が読めない
- **重要な変更時は、変更前後のスクリーンショット（PNG）を `assets/` にコミット**し、PR本文に貼る運用を推奨

### 4.4 命名規約

- ファイル名: kebab-case（`memo-list.pen` など）
- Pencil内のページ名: 画面名と一致させる（例: 「メモ一覧」「メモ新規作成」）

---

## 5. 実装との対応

- フロント実装時は `features/{機能名}/components/` のコンポーネントを Pencil のモックアップに合わせて作る
- Tailwind CSS のクラス設計時は、[`design.md`](./design.md) のデザイントークン（カラー・タイポグラフィ・スペーシング）を一次ソースとする
- Pencil 上の余白・配色は参考扱いとし、ピクセルパーフェクトは要求しない
- UI実装には **shadcn/ui**（`frontend/src/components/ui/` に取り込み） + **lucide-react** を採用する（詳細は [`design.md`](./design.md) §4）

---

## 6. 今後の検討事項

- [x] Pencil の再利用コンポーネント（ステンシル相当）の整備方針 → 2026-05-23 に整備完了。下記方針を採用
- [ ] フェーズ2の認証画面追加時のファイル分割タイミング

### 6.1 再利用コンポーネントの整備方針（2026-05-23 確定）

`design.pen` の「Design System」フレーム内で、以下のルールに沿って再利用コンポーネントを管理する。実装段階以降、新規追加・改変もこのルールに従う。

- **命名規約**: `Category/Variant` 形式。例: `Button/Primary` / `Toast/Success` / `PinToggle/On`
  - バリアントが 1 つのみのコンポーネントは Category だけで命名可（例: `MemoCard` / `Spinner` / `AppHeader`）
- **`reusable: true` のフラグ必須**。design.pen のリユーザブル一覧に表示されること
- **デザイントークン経由で値を参照**: 色は `$color-*` / 寸法は `$space-*` / 角丸は `$radius-*` / フォントは `$font-*` を `$` プレフィックスで参照（リテラル指定は避ける）
- **内部要素の名前付け**: インスタンス側から overrides で差し替えやすいよう、内部の子要素には意味のある `name` を付ける（例: `label` / `icon` / `message` / `placeholder` / `title` / `body`）
- **使用時のオーバーライド**: `ref` で参照し、`descendants` で内部要素のテキストや色を上書きする
- **整備済みコンポーネント**: 24 種類（2026-05-23 時点）。詳細は `design.pen` の「Design System」フレームを参照

> デザイントークン（色・余白・タイポグラフィ）は [`design.md`](./design.md) に集約済み。
